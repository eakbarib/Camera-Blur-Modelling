# Camera-Blur-Modelling

Calibrating camera + lens depth of field blur

## Setup

`py -m venv .venv`
`.venv/Scripts/activate`
`pip install -r requirements.txt`

put the calibration data in `/bokeh_calib_photos`
generate image stacks using `gen_stacks()` in `load_data.py`